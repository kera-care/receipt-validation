# GLM-OCR Fine-Tune

Fine-tune [zai-org/GLM-OCR](https://huggingface.co/zai-org/GLM-OCR) for receipt validation from receipt images. Includes training (full fine-tune + LoRA), distributed inference, and a multi-level evaluation pipeline with root-based matching and exclusion detection.

